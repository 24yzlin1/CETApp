<t-message id="t-message" />
<t-dialog
bindconfirm="loginHandle"
confirm-btn="{{loginButton}}"
visible="{{!isLogin}}"
></t-dialog>
<view wx:if="{{isLogin}}">
<body wx:if="{{isShown}}" />
<body wx:else />
</view>
